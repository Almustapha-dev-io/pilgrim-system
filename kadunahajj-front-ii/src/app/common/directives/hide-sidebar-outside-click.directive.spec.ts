import { HideSidebarOutsideClickDirective } from './hide-sidebar-outside-click.directive';

describe('HideSidebarOutsideClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HideSidebarOutsideClickDirective();
    expect(directive).toBeTruthy();
  });
});
