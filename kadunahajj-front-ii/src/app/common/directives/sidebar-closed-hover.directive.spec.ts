import { SidebarClosedHoverDirective } from './sidebar-closed-hover.directive';

describe('SidebarClosedHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarClosedHoverDirective();
    expect(directive).toBeTruthy();
  });
});
